# Ethiopia Financial Inclusion Forecasting Dashboard

[cite_start]An interactive, professional-grade visual interface developed in Streamlit to explore historical trends, track event impacts, and project financial inclusion rates for Ethiopia (2025–2027)[cite: 256, 286, 289].

---

### 1. Local Development Setup

To configure your environment and launch the dashboard application, run the following commands:

```bash
# Clone the repository and navigate to the project directory
cd ethiopia-fi-forecast

# Create and activate a virtual environment
python -m venv venv
source venv/bin/activate  # On Windows use: venv\Scripts\activate

# Install the required technical libraries
pip install -r requirements.txt